<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link rel="stylesheet" type="text/css" href="../css/normalize.css">
  <link rel="stylesheet" type="text/css" href="../css/style.css">
  <title>OAuth</title>
</head>
<body>
    <div id="container">
        <div class="header">
            <a href="../"><img src="../img/rs.svg" alt="remoteStorage"></a>
            <!-- <h1>remoteStorage</h1> -->
        </div>

        <p>
        This is a list of applications you gave access to your <a href="https://remotestorage.io/">remoteStorage</a>
        data. If you revoke the access, the application can no longer use the 
        issued tokens and has to ask your permission again!
        </p>

        {% if not approvalList %}
            <p class="plain">No permissions granted yet! You need to use an application first :-)</p>
        {% else %}
            <table>
                <tr><th>Redirect URI</th><th>Permissions</th><th></th></tr>
                {% for approval in approvalList %}
                <tr>
                    <td><code>{{ approval.getRedirectUri|e }}</code></td>
                    <td><code>{{ approval.getScope|e }}</code></td>
                    <td>
                        <form method="post" action="?client_id={{ approval.getClientId|e }}&redirect_uri={{ approval.getRedirectUri|e }}&response_type={{ approval.getResponseType|e }}&scope={{ approval.getScope|e }}">
                            <input type="hidden" name="_METHOD" value="DELETE">
                            <button type="submit" class="error">Revoke</button>
                        </form>
                    </td>
                </tr>
                {% endfor %}
            </table>
        {% endif %}

<!--
        <form method="post" action="../_auth/form/logout">
            <button type="submit" class="warning">Logout</button>
        </form> 
-->
    </div> <!-- /container -->
</body>
</html>
