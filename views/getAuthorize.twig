<!DOCTYPE html>

<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <link rel="stylesheet" type="text/css" href="../css/normalize.css">
  <link rel="stylesheet" type="text/css" href="../css/style.css">
  <title>OAuth</title>
</head>
<body>
    <div id="container">
        <div class="header">
            <a href="../"><img src="../img/rs.svg" alt="remoteStorage"></a>
        </div>

<!--
        {% if not has_state %}
            <p class="warning">
                The client does not use the <code>state</code> parameter to 
                avoid CSRF! This is <a href="https://github.com/remotestorage/remotestorage.js/issues/900">probably</a> secure in case of 
                <a href="https://github.com/remotestorage/remotestorage.js">remotestorage.js</a>.
            </p>
        {% endif %}
-->
        <p>
            You are currently logged in as <strong>{{ user_id|e }}</strong>.
        </p>

        <!-- switch user -->
<!--        <form method="post" action="../_auth/form/logout">
            <input type="hidden" name="redirect_to" value ="{{ request_url|e }}">
            <button type="submit">Switch User</button>
        </form> -->


        <p>An application wants to access your <a href="https://remotestorage.io/">remoteStorage</a> data.</p>

        <table>
            <tr><th>Redirect URI</th><td><code>{{ redirect_uri|e }}</code></td></tr>
            <tr><th>Permissions</th><td><code>{{ scope|e }}</code></td></tr>
        </table>

        <form method="post" action="{{ request_url|e }}">
            <button class="error" type="submit" name="approval" value="no">Reject</button>
            <button class="info"  type="submit" name="approval" value="yes" autofocus>Approve</button>
        </form>
    </div> <!-- /container -->
</body>
</html>
